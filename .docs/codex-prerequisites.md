# Codex prerequisites

- Install Codex CLI so `codex` is on your PATH.
- Authenticate Codex before running BIDE code (for example via API key or ChatGPT auth supported by Codex).
- BIDE code starts the server via `codex app-server` per session.
